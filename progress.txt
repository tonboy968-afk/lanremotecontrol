## Codebase Patterns
- Project uses markdown documentation for architecture and design specs in the docs/ directory
- Host service code goes in src-host/, client application code goes in src-client/
- Screen capture uses OS-specific native APIs (DXGI on Windows, ScreenCaptureKit on macOS) to minimize overhead
- Delta frame detection uses tile-based partitioning and hash-based change detection with a threshold-based full frame fallback (>30% tiles changed)
- Adaptive switching between LZ4 raw delta and low-delay H.264/AV1 is based on network RTT, packet loss, CPU/GPU utilization, and content type detection
- Input capture uses OS-specific low-level APIs (WH_KEYBOARD_LL/WH_MOUSE_LL on Windows, CGEventTap on macOS, evdev/X11 on Linux)
- Host input injection via SendInput requires proper foreground focus handling; modifier key state synchronization is critical to prevent stuck keys due to UDP packet loss
- Input events are serialized with type, high-resolution timestamp, and sequence numbers; host applies coalescing for rapid mouse movements and maintains key state maps to handle UDP unordered/duplicated packets
- **Rust project structure:** Cargo workspace with three crates: `common` (shared message types, serialization, protocol constants), `host` (host service binary), `client` (client application binary)
- **Message serialization:** Uses serde + bincode for compact binary serialization; message header is 11 bytes (MessageType + sequence_number + payload_length + reserved)
- **Workspace dependency management:** Common dependencies (serde, bincode) defined at workspace level and inherited by member crates

## [2026-07-30] - US-009
- Implemented UDP network listener and connection establishment
- Files changed: host/src/net.rs, client/src/net.rs, host/src/main.rs, client/src/main.rs, host/Cargo.toml
- **Learnings for future iterations:**
  - Patterns discovered: Use `#[cfg(unix)]` for platform-specific socket options (SO_REUSEADDR, TOS); `force_tick` helper pattern for testing time-based heartbeat manager without wall-clock dependency
  - Gotchas encountered: `UdpSocket::set_reuseaddr()` and `set_tos()` are Unix-only — on Windows they don't exist and require `#[cfg(unix)]` guards; sending to `0.0.0.0:port` is not routable on Windows (must use `127.0.0.1` in tests); instant-based tick tests are fragile under parallel test execution due to timer resolution — reset `last_tick` before each call to guarantee tick fires
  - Useful context: `UdpSocket::connect()` on client side allows using `send()`/`recv()` without addresses; `wrapping_sub` handles heartbeat sequence number wraparound; bincode serialization errors convert to `io::Error` via `map_err`
---

## [2026-07-27 23:22:00] - US-005
- What was implemented
- Created docs/UI_UX_DESIGN.md with connection panel UI components (IP input, Connect button, status indicator)
- Documented remote control window UI (frame display area, fullscreen toggle, disconnect button)
- Defined interaction flow and state transitions for connection and remote control session
- **Learnings for future iterations:**
  - Patterns discovered: Connection panel uses minimal vertical stack layout with IP input, Connect button, and status indicator; Remote control window prioritizes maximum screen real estate with overlay controls that auto-hide after inactivity
  - Useful context: UI framework choices should prioritize low memory footprint and fast render times to avoid impacting remote control performance; aspect ratio preservation is maintained via black bars when client/host ratios differ
---
## [2026-07-30 16:15:00] - US-008
- What was implemented
  - Created Rust Cargo workspace under `lanremotecontrol/` with workspace root Cargo.toml
  - Implemented `common/` library crate with core message types (MessageType, Message, control command sub-types, connection management sub-types)
  - Implemented protocol constants (DEFAULT_PORT, HEARTBEAT_INTERVAL_MS, MAX_PACKET_SIZE, ACK_TIMEOUT_MS, MAX_RETRANSMIT)
  - Implemented session state types (ConnectionState enum, SessionConfig struct)
  - Implemented serialization using serde + bincode with round-trip tests
  - Created host binary crate (`host/`) and client binary crate (`client/`)
  - Added .gitignore for Rust build artifacts
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace root)
  - `lanremotecontrol/common/Cargo.toml`, `lanremotecontrol/common/src/lib.rs`
  - `lanremotecontrol/host/Cargo.toml`, `lanremotecontrol/host/src/main.rs`
  - `lanremotecontrol/client/Cargo.toml`, `lanremotecontrol/client/src/main.rs`
  - `lanremotecontrol/.gitignore`
- **Learnings for future iterations:**
  - Patterns discovered: Workspace resolver = "2" is needed for clean dependency resolution; use workspace-level [workspace.dependencies] to share dependency versions across packages
  - Gotchas encountered: bincode 1.x must be explicitly specified (3.x is latest but breaking); Message header size is 11 bytes (1+4+4+2) per protocol spec — important for MTU calculations
  - Useful context: The common crate is named `lanremotecontrol-common` to avoid naming conflicts; host and client crates depend on it via path dependency
---

## [2026-07-30] - US-010
- What was implemented
  - **Screen capture trait** in `common/src/capture.rs`: `ScreenCapture` trait, `CapturedFrame` struct, `CaptureError` enum
  - **Tile-based delta detection and LZ4 compression** in `common/src/encoding.rs`:
    - `tile_checksums()` — XXH32 hash per 64×64 tile
    - `detect_delta_tiles()` — find changed tiles between frames
    - `extract_tile_data()` — extract pixel data for a tile
    - `compress_delta()` / `decompress_delta()` — round-trip for delta regions
    - `compress_full_frame()` / `decompress_full_frame()` — full frame LZ4
    - `should_send_full_frame()` — threshold-based (>30%) fallback
    - `build_delta_regions()` — build delta regions from changed tiles
    - Comprehensive tests for encoding/decoding round-trips (all pass)
  - **Windows DXGI Desktop Duplication** in `host/src/capture.rs`:
    - `DxgiCapture` struct implementing `ScreenCapture` trait
    - D3D11 device creation (tries HW then WARP driver)
    - AcquireNextFrame with staging texture copy
    - Row-by-row pixel copy handling stride differences
    - `#[cfg(windows)]` guard with non-Windows stub returning `UnsupportedOs`
  - **Host main.rs demo**: initializes capture on startup, prints display info, tile counts, and LZ4 compression ratio
  - **Workspace dependencies**: Added `lz4`, `xxhash-rust`, `windows-core`, and `windows` (with `Win32_Graphics_Dxgi`, `Win32_Graphics_Direct3D`, `Win32_Graphics_Direct3D11`, `Win32_Graphics_Dxgi_Common`, `Win32_System_Com`, `Win32_Graphics_Gdi`, `Win32_Foundation`)
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace deps)
  - `lanremotecontrol/common/Cargo.toml`
  - `lanremotecontrol/host/Cargo.toml`
  - `lanremotecontrol/common/src/lib.rs` (added `pub mod capture;` and `pub mod encoding;`)
  - `lanremotecontrol/common/src/capture.rs` (new)
  - `lanremotecontrol/common/src/encoding.rs` (new)
  - `lanremotecontrol/host/src/capture.rs` (new)
  - `lanremotecontrol/host/src/main.rs` (added `mod capture;` and `demo_screen_capture()`)
  - `lanremotecontrol/Cargo.lock` (auto)
- **Learnings for future iterations:**
  - Patterns discovered: The `windows` crate v0.58 uses a distinctive COM API style where methods like `EnumOutputs(u32) -> Result<IDXGIOutput>` return the interface directly, not via output parameters. Types like `D3D_DRIVER_TYPE` live in `Win32_Graphics_Direct3D` module, NOT `Direct3D11`. The `Interface` trait with `.cast()` is in `windows::core::Interface`.
  - Gotchas encountered (Windows-specific DXGI issues!):
    1. `D3D_DRIVER_TYPE_HARDWARE` and `D3D_DRIVER_TYPE_WARP` are in `windows::Win32::Graphics::Direct3D` (not `Direct3D11`)
    2. `DuplicateOutput()` is on `IDXGIOutput1` (not `IDXGIOutput`) — need `.cast::<IDXGIOutput1>()` first
    3. `HMODULE` wrapper is required for the software rasterizer parameter: `HMODULE(std::ptr::null_mut())` instead of `None`
    4. `D3D11CreateDevice` signature: no separate `featurelevels: u32` param; `pfeaturelevels: None` handles both pointer and count
    5. `ID3D11Texture2D::GetDesc(&self, pdesc: *mut D3D11_TEXTURE2D_DESC)` takes an output pointer (returns `()`), not self-returning
    6. `D3D11_CPU_ACCESS_READ.0` is `i32` but `D3D11_TEXTURE2D_DESC.CPUAccessFlags` expects `u32` — need `.0 as u32` cast
    7. `BindFlags`, `CPUAccessFlags`, `MiscFlags` are raw `u32` fields, not the flag-wrapping types in the struct literal
  - Useful context: XXH32 is fast but for uniform frames edge tiles naturally have different hashes due to different data sizes; LZ4 may not compress high-entropy (pattern/random) data smaller than original — tests should verify round-trip correctness, not compression ratio
  - Updating dependencies: `windows-core` must be added as an explicit workspace + host dependency when using `windows::core::Interface`
---

## [2026-07-30] - US-011
- What was implemented
  - **Client-side input capture** in `client/src/input.rs`:
    - `InputCapture` struct with `GetAsyncKeyState` polling for all VK codes 0x01–0xFF
    - Keyboard state tracking: previous vs current to detect press/release transitions
    - Mouse position tracking via `GetCursorPos` with relative delta computation
    - Mouse button state tracking (left, right, middle, X1, X2) via `GetAsyncKeyState`
    - Modifier key state packing (Shift/Ctrl/Alt/Win) into u8 bitmask
    - High-resolution timestamp (microseconds since UNIX epoch) per event
    - Sequence number counter (`next_seq()`) for ordering
    - `#[cfg(windows)]` guard with non-Windows stub returning empty vectors
  - **Host-side input injection** in `host/src/input.rs`:
    - `InputInjector` struct with `inject()` and `inject_batch()` methods
    - Keyboard injection via `SendInput` with `KEYBDINPUT` (VIRTUAL_KEY, KEYBD_EVENT_FLAGS)
    - Mouse movement injection: relative (MOUSEEVENTF_MOVE) and absolute (MOUSEEVENTF_MOVE|MOUSEEVENTF_ABSOLUTE)
    - Mouse button injection: left/right/middle/X1/X2 down/up events
    - Scroll injection: vertical (MOUSEEVENTF_WHEEL) and horizontal (MOUSEEVENTF_HWHEEL)
    - 6 unit tests for payload-to-INPUT conversion logic
    - `#[cfg(windows)]` guard with non-Windows stub returning Unsupported error
- Files changed
  - `lanremotecontrol/Cargo.toml` (added `Win32_UI_Input_KeyboardAndMouse`, `Win32_UI_WindowsAndMessaging`)
  - `lanremotecontrol/client/Cargo.toml` (added `windows` dependency)
  - `lanremotecontrol/client/src/input.rs` (NEW — input capture module)
  - `lanremotecontrol/client/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/host/src/input.rs` (NEW — input injection module)
  - `lanremotecontrol/host/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-011 passes: true)
- **Learnings for future iterations:**
  - Patterns discovered: windows-rs v0.58 API — `GetAsyncKeyState(vkey: i32)` takes i32 not VIRTUAL_KEY; `GetCursorPos(lppoint: *mut POINT)` takes output pointer, returns Result<()>; `SendInput(pinputs: &[INPUT], cbsize: i32)` takes slice. `KEYBDINPUT.wVk` is VIRTUAL_KEY(u16), `dwFlags` is KEYBD_EVENT_FLAGS(u32). KEYEVENTF_KEYDOWN = 0 (no separate constant). `dwExtraInfo` is usize, not *mut c_void.
  - Gotchas encountered:
    1. `GetAsyncKeyState` takes `i32` directly (not `VIRTUAL_KEY`), even though `KEYBDINPUT.wVk` uses `VIRTUAL_KEY`
    2. `0x8000` literal doesn't fit in `i16` — must cast to `u16` first: `(state as u16) & 0x8000 != 0`
    3. `GetCursorPos` requires an output pointer `&mut POINT` and returns `Result<()>`, not directly the point
    4. `dwExtraInfo` in both `KEYBDINPUT` and `MOUSEINPUT` is `usize`, not `*mut c_void` — use `0usize`
    5. There is no `KEYEVENTF_KEYDOWN` constant in windows-rs 0.58 (value 0) — use `KEYBD_EVENT_FLAGS(0)`
  - Useful context: `INPUT_0` is a union-like struct with `mi`, `ki`, `hi` fields; using `Default::default()` zero-initializes it. `POINT` is in `windows::Win32::Foundation`. The SendInput batch approach (single call with many INPUTs) ensures atomic injection of related events (e.g., simultaneous mouse button + position).
---

## [2026-07-30] - US-012
- What was implemented
  - **Minimalist GUI** using egui 0.27 + eframe 0.27 in `client/src/ui.rs`:
    - `RemoteControlApp` struct implementing `eframe::App` trait
    - `AppState` enum: Disconnected, Connecting, Connected {encoding, width, height}, Error(String)
    - Connection panel with IP input field (TextEdit), port input field, colored status indicator, and Connect/Disconnect button
    - Remote control window with dark background, centered connection info text, FPS/latency info bottom-right
    - Fullscreen toggle and Disconnect overlay buttons in top-right corner
    - Error panel with retry button when handshake/connection fails
    - Async handshake via `std::thread::spawn` with `Arc<Mutex<Option<Result>>>` channel
    - `ctx.request_repaint()` for smooth animation during connecting state
  - **Updated `client/src/main.rs`**: replaced CLI logic with `eframe::run_native()` call
  - **Updated `client/Cargo.toml`**: added `eframe = { version = "0.27", features = ["default_system_theme", "glow"] }` and `egui = "0.27"`
  - Added `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` to hide console window in release builds
- Files changed
  - `lanremotecontrol/client/Cargo.toml` (added eframe, egui deps)
  - `lanremotecontrol/client/src/ui.rs` (NEW — egui UI module)
  - `lanremotecontrol/client/src/main.rs` (rewritten to use eframe)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-012 passes: true)
- **Build result**: `cargo build` passes with 6 warnings (unused imports/dead code from input.rs and net.rs — expected until full integration with US-013)
- **Test result**: All 57 tests pass (2 client + 39 common + 16 host)
- **Learnings for future iterations:**
  - Patterns discovered: eframe 0.27 uses `eframe::App::update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame)`. Use `egui::CentralPanel` for the main content area. `ui.vertical_centered()` with `egui::Frame::group()` for structured panels. `ctx.send_viewport_cmd(egui::ViewportCommand::Fullscreen(true/false))` for fullscreen toggling. `ctx.request_repaint()` needed for continuous updates during connecting state.
  - Gotchas encountered:
    1. eframe 0.27's `eframe::NativeOptions` has no `always_on_top`, `decorated` etc. directly — use `viewport: eframe::egui::ViewportBuilder::default()` for window properties
    2. `egui::ViewportCommand::Fullscreen(bool)` replaces the older `Frame::set_fullscreen()` API
    3. `egui::CentralPanel` is not an `egui::Panel` — it's a free function `egui::CentralPanel::default().show(ctx, |ui| {...})`
    4. For overlay buttons over the screen area, use `ui.put(rect, widget)` with absolute positions calculated from `ui.max_rect()`
    5. Can't borrow `self` mutably multiple times in `update()` — need to match on state clone or restructure logic to avoid borrow conflicts
  - Useful context: `egui::Color32::from_rgba_premultiplied(r,g,b,a)` for transparency on overlay buttons. `ui.colored_label()` replaces manual text painting for simple labels. `ui.fonts(|f| f.layout_no_wrap(...))` for measuring text bounds before drawing. `egui::Vec2` is aliased at top level. `egui::pos2` and `egui::rect` for geometry. `ui.add_sized([w, h], widget)` for fixed-size widgets.
---

## [2026-07-30] - US-013
- What was implemented
  - **11 integration tests** in `lanremotecontrol/common/tests/common_integration.rs`:
    - `test_full_connection_handshake_simulated` — full 4-step handshake (request → capabilities → confirm → teardown) including rejected variant
    - `test_screen_encode_decode_pipeline` — full pipeline (frame creation → tile_checksums → detect_delta_tiles → build_delta_regions → compress_delta → decompress_delta → verify) with gradient frame and half-frame modification
    - `test_large_frame_4k_compression` — 3840×2160 checkerboard frame with timing measurements for tile checksums, LZ4 compress/decompress, delta compress/decompress, and compression ratio verification
    - `test_input_event_serialize_deserialize_pipeline` — all 4 input event types (KeyEvent, MouseMoveEvent, MouseButtonEvent, ScrollEvent) through full serialize→deserialize→verify pipeline with 8 test cases
    - `test_multi_message_stream_round_trip` — concatenated stream of 8 mixed messages (heartbeats, ACKs, connection management) verified round-trip
    - `test_large_payload_message` — messages with 5 different payload sizes (0, 1, MTU, 4×MTU, 100KB)
    - `test_captured_frame_to_encoding_pipeline` — CapturedFrame→encoding pipeline with simulated cursor change
    - `test_multiple_frames_sequential_delta` — 3-frame sequential delta detection (baseline → small cursor → larger change)
    - `test_various_encoding_capabilities_configs` — 4 encoding capability configurations serialized and verified
    - `test_message_boundary_conditions` — min message, max sequence number, all message types
    - `test_very_small_frame_encoding` — 100×100 frame edge case with single-pixel modification
  - **Benchmark document** `docs/BENCHMARK_RESULTS.md`:
    - Test environment documentation
    - Latency measurements for tile checksums, LZ4 compression/decompression (720p & 4K), delta operations
    - Compression ratios for various content types (gradient, checkerboard, uniform, delta)
    - Network protocol timing estimates
    - End-to-end pipeline latency estimates (3–10 ms best case on LAN)
    - 8 performance tuning recommendations: tile size adaptation, adaptive encoding selection, thread affinity, network buffer tuning, GPU direct transfer (DXGI), frame pacing, serialisation optimisation, build profile recommendations
- Files changed
  - `lanremotecontrol/common/tests/common_integration.rs` (NEW — 11 integration tests)
  - `docs/BENCHMARK_RESULTS.md` (NEW — benchmark methodology and results)
  - `prd.json` (US-013 passes: true)
- **Build result**: `cargo build` passes (same 6 warnings as before)
- **Test result**: All 68 tests pass (57 existing unit + 11 new integration) across all crates
- **Learnings for future iterations:**
  - Patterns discovered: Integration tests in `common/tests/` are auto-discovered by Cargo for library crates; bincode adds 22 bytes of framing overhead (4 enum tag + 4 seq + 4 payload_len + 2 reserved + 8 Vec length prefix), not the 11-byte protocol header_size; the serde `#[repr(u8)]` attribute on MessageType affects the wire format differently than bincode's own enum encoding
  - Gotchas encountered:
    1. bincode does NOT use `#[repr(u8)]` for enum serialization — it encodes enums as u32 (4 bytes) by default, not 1 byte, so the actual serialized message overhead is 22 bytes vs the 11-byte protocol header
    2. The concatenated stream test requires knowing each message's exact serialized length (from prior serialization) — cannot use `header_size() + payload_length` directly because bincode adds Vec length prefix
    3. 4K frame (3840×2160 → ~31.6 MB) round-trip through LZ4 works correctly in debug mode with reasonable performance (a few ms for compress+decompress)
    4. Checkerboard patterns compress very well with LZ4 (>10×), while gradient/random patterns may not compress much (<2×) — this is expected behavior for LZ4
  - Useful context: Cargo's `tests/` directory is auto-discovered for library crates; `cargo test -p lanremotecontrol-common --test common_integration` runs just the integration tests; `std::time::Instant` is adequate for millisecond-level benchmarks; delta detection with 64×64 tiles on 4K produces 2040 tiles, manageable in <20ms even in debug mode

## [2026-07-27 22:32:00] - US-001
- What was implemented
- Created project root directory structure (docs/, src-host/, src-client/)
- Created docs/ARCHITECTURE.md with system components and data flow diagram
- **Learnings for future iterations:**
  - Patterns discovered: Project uses markdown documentation for architecture and design specs
  - Useful context: Host service code goes in src-host/, client application code goes in src-client/
## [2026-07-27 23:22:00] - US-005
- What was implemented
- Created docs/UI_UX_DESIGN.md with connection panel UI components (IP input, Connect button, status indicator)
- Documented remote control window UI (frame display area, fullscreen toggle, disconnect button)
- Defined interaction flow and state transitions for connection and remote control session
- **Learnings for future iterations:**
  - Patterns discovered: Connection panel uses minimal vertical stack layout with IP input, Connect button, and status indicator; Remote control window prioritizes maximum screen real estate with overlay controls that auto-hide after inactivity
  - Useful context: UI framework choices should prioritize low memory footprint and fast render times to avoid impacting remote control performance; aspect ratio preservation is maintained via black bars when client/host ratios differ
---
## [2026-07-30 16:15:00] - US-008
- What was implemented
  - Created Rust Cargo workspace under `lanremotecontrol/` with workspace root Cargo.toml
  - Implemented `common/` library crate with core message types (MessageType, Message, control command sub-types, connection management sub-types)
  - Implemented protocol constants (DEFAULT_PORT, HEARTBEAT_INTERVAL_MS, MAX_PACKET_SIZE, ACK_TIMEOUT_MS, MAX_RETRANSMIT)
  - Implemented session state types (ConnectionState enum, SessionConfig struct)
  - Implemented serialization using serde + bincode with round-trip tests
  - Created host binary crate (`host/`) and client binary crate (`client/`)
  - Added .gitignore for Rust build artifacts
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace root)
  - `lanremotecontrol/common/Cargo.toml`, `lanremotecontrol/common/src/lib.rs`
  - `lanremotecontrol/host/Cargo.toml`, `lanremotecontrol/host/src/main.rs`
  - `lanremotecontrol/client/Cargo.toml`, `lanremotecontrol/client/src/main.rs`
  - `lanremotecontrol/.gitignore`
- **Learnings for future iterations:**
  - Patterns discovered: Workspace resolver = "2" is needed for clean dependency resolution; use workspace-level [workspace.dependencies] to share dependency versions across packages
  - Gotchas encountered: bincode 1.x must be explicitly specified (3.x is latest but breaking); Message header size is 11 bytes (1+4+4+2) per protocol spec — important for MTU calculations
  - Useful context: The common crate is named `lanremotecontrol-common` to avoid naming conflicts; host and client crates depend on it via path dependency
---

## [2026-07-30] - US-010
- What was implemented
  - **Screen capture trait** in `common/src/capture.rs`: `ScreenCapture` trait, `CapturedFrame` struct, `CaptureError` enum
  - **Tile-based delta detection and LZ4 compression** in `common/src/encoding.rs`:
    - `tile_checksums()` — XXH32 hash per 64×64 tile
    - `detect_delta_tiles()` — find changed tiles between frames
    - `extract_tile_data()` — extract pixel data for a tile
    - `compress_delta()` / `decompress_delta()` — round-trip for delta regions
    - `compress_full_frame()` / `decompress_full_frame()` — full frame LZ4
    - `should_send_full_frame()` — threshold-based (>30%) fallback
    - `build_delta_regions()` — build delta regions from changed tiles
    - Comprehensive tests for encoding/decoding round-trips (all pass)
  - **Windows DXGI Desktop Duplication** in `host/src/capture.rs`:
    - `DxgiCapture` struct implementing `ScreenCapture` trait
    - D3D11 device creation (tries HW then WARP driver)
    - AcquireNextFrame with staging texture copy
    - Row-by-row pixel copy handling stride differences
    - `#[cfg(windows)]` guard with non-Windows stub returning `UnsupportedOs`
  - **Host main.rs demo**: initializes capture on startup, prints display info, tile counts, and LZ4 compression ratio
  - **Workspace dependencies**: Added `lz4`, `xxhash-rust`, `windows-core`, and `windows` (with `Win32_Graphics_Dxgi`, `Win32_Graphics_Direct3D`, `Win32_Graphics_Direct3D11`, `Win32_Graphics_Dxgi_Common`, `Win32_System_Com`, `Win32_Graphics_Gdi`, `Win32_Foundation`)
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace deps)
  - `lanremotecontrol/common/Cargo.toml`
  - `lanremotecontrol/host/Cargo.toml`
  - `lanremotecontrol/common/src/lib.rs` (added `pub mod capture;` and `pub mod encoding;`)
  - `lanremotecontrol/common/src/capture.rs` (new)
  - `lanremotecontrol/common/src/encoding.rs` (new)
  - `lanremotecontrol/host/src/capture.rs` (new)
  - `lanremotecontrol/host/src/main.rs` (added `mod capture;` and `demo_screen_capture()`)
  - `lanremotecontrol/Cargo.lock` (auto)
- **Learnings for future iterations:**
  - Patterns discovered: The `windows` crate v0.58 uses a distinctive COM API style where methods like `EnumOutputs(u32) -> Result<IDXGIOutput>` return the interface directly, not via output parameters. Types like `D3D_DRIVER_TYPE` live in `Win32_Graphics_Direct3D` module, NOT `Direct3D11`. The `Interface` trait with `.cast()` is in `windows::core::Interface`.
  - Gotchas encountered (Windows-specific DXGI issues!):
    1. `D3D_DRIVER_TYPE_HARDWARE` and `D3D_DRIVER_TYPE_WARP` are in `windows::Win32::Graphics::Direct3D` (not `Direct3D11`)
    2. `DuplicateOutput()` is on `IDXGIOutput1` (not `IDXGIOutput`) — need `.cast::<IDXGIOutput1>()` first
    3. `HMODULE` wrapper is required for the software rasterizer parameter: `HMODULE(std::ptr::null_mut())` instead of `None`
    4. `D3D11CreateDevice` signature: no separate `featurelevels: u32` param; `pfeaturelevels: None` handles both pointer and count
    5. `ID3D11Texture2D::GetDesc(&self, pdesc: *mut D3D11_TEXTURE2D_DESC)` takes an output pointer (returns `()`), not self-returning
    6. `D3D11_CPU_ACCESS_READ.0` is `i32` but `D3D11_TEXTURE2D_DESC.CPUAccessFlags` expects `u32` — need `.0 as u32` cast
    7. `BindFlags`, `CPUAccessFlags`, `MiscFlags` are raw `u32` fields, not the flag-wrapping types in the struct literal
  - Useful context: XXH32 is fast but for uniform frames edge tiles naturally have different hashes due to different data sizes; LZ4 may not compress high-entropy (pattern/random) data smaller than original — tests should verify round-trip correctness, not compression ratio
  - Updating dependencies: `windows-core` must be added as an explicit workspace + host dependency when using `windows::core::Interface`
---

## [2026-07-30] - US-011
- What was implemented
  - **Client-side input capture** in `client/src/input.rs`:
    - `InputCapture` struct with `GetAsyncKeyState` polling for all VK codes 0x01–0xFF
    - Keyboard state tracking: previous vs current to detect press/release transitions
    - Mouse position tracking via `GetCursorPos` with relative delta computation
    - Mouse button state tracking (left, right, middle, X1, X2) via `GetAsyncKeyState`
    - Modifier key state packing (Shift/Ctrl/Alt/Win) into u8 bitmask
    - High-resolution timestamp (microseconds since UNIX epoch) per event
    - Sequence number counter (`next_seq()`) for ordering
    - `#[cfg(windows)]` guard with non-Windows stub returning empty vectors
  - **Host-side input injection** in `host/src/input.rs`:
    - `InputInjector` struct with `inject()` and `inject_batch()` methods
    - Keyboard injection via `SendInput` with `KEYBDINPUT` (VIRTUAL_KEY, KEYBD_EVENT_FLAGS)
    - Mouse movement injection: relative (MOUSEEVENTF_MOVE) and absolute (MOUSEEVENTF_MOVE|MOUSEEVENTF_ABSOLUTE)
    - Mouse button injection: left/right/middle/X1/X2 down/up events
    - Scroll injection: vertical (MOUSEEVENTF_WHEEL) and horizontal (MOUSEEVENTF_HWHEEL)
    - 6 unit tests for payload-to-INPUT conversion logic
    - `#[cfg(windows)]` guard with non-Windows stub returning Unsupported error
- Files changed
  - `lanremotecontrol/Cargo.toml` (added `Win32_UI_Input_KeyboardAndMouse`, `Win32_UI_WindowsAndMessaging`)
  - `lanremotecontrol/client/Cargo.toml` (added `windows` dependency)
  - `lanremotecontrol/client/src/input.rs` (NEW — input capture module)
  - `lanremotecontrol/client/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/host/src/input.rs` (NEW — input injection module)
  - `lanremotecontrol/host/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-011 passes: true)
- **Learnings for future iterations:**
  - Patterns discovered: windows-rs v0.58 API — `GetAsyncKeyState(vkey: i32)` takes i32 not VIRTUAL_KEY; `GetCursorPos(lppoint: *mut POINT)` takes output pointer, returns Result<()>; `SendInput(pinputs: &[INPUT], cbsize: i32)` takes slice. `KEYBDINPUT.wVk` is VIRTUAL_KEY(u16), `dwFlags` is KEYBD_EVENT_FLAGS(u32). KEYEVENTF_KEYDOWN = 0 (no separate constant). `dwExtraInfo` is usize, not *mut c_void.
  - Gotchas encountered:
    1. `GetAsyncKeyState` takes `i32` directly (not `VIRTUAL_KEY`), even though `KEYBDINPUT.wVk` uses `VIRTUAL_KEY`
    2. `0x8000` literal doesn't fit in `i16` — must cast to `u16` first: `(state as u16) & 0x8000 != 0`
    3. `GetCursorPos` requires an output pointer `&mut POINT` and returns `Result<()>`, not directly the point
    4. `dwExtraInfo` in both `KEYBDINPUT` and `MOUSEINPUT` is `usize`, not `*mut c_void` — use `0usize`
    5. There is no `KEYEVENTF_KEYDOWN` constant in windows-rs 0.58 (value 0) — use `KEYBD_EVENT_FLAGS(0)`
  - Useful context: `INPUT_0` is a union-like struct with `mi`, `ki`, `hi` fields; using `Default::default()` zero-initializes it. `POINT` is in `windows::Win32::Foundation`. The SendInput batch approach (single call with many INPUTs) ensures atomic injection of related events (e.g., simultaneous mouse button + position).
---

## [2026-07-30] - US-012
- What was implemented
  - **Minimalist GUI** using egui 0.27 + eframe 0.27 in `client/src/ui.rs`:
    - `RemoteControlApp` struct implementing `eframe::App` trait
    - `AppState` enum: Disconnected, Connecting, Connected {encoding, width, height}, Error(String)
    - Connection panel with IP input field (TextEdit), port input field, colored status indicator, and Connect/Disconnect button
    - Remote control window with dark background, centered connection info text, FPS/latency info bottom-right
    - Fullscreen toggle and Disconnect overlay buttons in top-right corner
    - Error panel with retry button when handshake/connection fails
    - Async handshake via `std::thread::spawn` with `Arc<Mutex<Option<Result>>>` channel
    - `ctx.request_repaint()` for smooth animation during connecting state
  - **Updated `client/src/main.rs`**: replaced CLI logic with `eframe::run_native()` call
  - **Updated `client/Cargo.toml`**: added `eframe = { version = "0.27", features = ["default_system_theme", "glow"] }` and `egui = "0.27"`
  - Added `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` to hide console window in release builds
- Files changed
  - `lanremotecontrol/client/Cargo.toml` (added eframe, egui deps)
  - `lanremotecontrol/client/src/ui.rs` (NEW — egui UI module)
  - `lanremotecontrol/client/src/main.rs` (rewritten to use eframe)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-012 passes: true)
- **Build result**: `cargo build` passes with 6 warnings (unused imports/dead code from input.rs and net.rs — expected until full integration with US-013)
- **Test result**: All 57 tests pass (2 client + 39 common + 16 host)
- **Learnings for future iterations:**
  - Patterns discovered: eframe 0.27 uses `eframe::App::update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame)`. Use `egui::CentralPanel` for the main content area. `ui.vertical_centered()` with `egui::Frame::group()` for structured panels. `ctx.send_viewport_cmd(egui::ViewportCommand::Fullscreen(true/false))` for fullscreen toggling. `ctx.request_repaint()` needed for continuous updates during connecting state.
  - Gotchas encountered:
    1. eframe 0.27's `eframe::NativeOptions` has no `always_on_top`, `decorated` etc. directly — use `viewport: eframe::egui::ViewportBuilder::default()` for window properties
    2. `egui::ViewportCommand::Fullscreen(bool)` replaces the older `Frame::set_fullscreen()` API
    3. `egui::CentralPanel` is not an `egui::Panel` — it's a free function `egui::CentralPanel::default().show(ctx, |ui| {...})`
    4. For overlay buttons over the screen area, use `ui.put(rect, widget)` with absolute positions calculated from `ui.max_rect()`
    5. Can't borrow `self` mutably multiple times in `update()` — need to match on state clone or restructure logic to avoid borrow conflicts
  - Useful context: `egui::Color32::from_rgba_premultiplied(r,g,b,a)` for transparency on overlay buttons. `ui.colored_label()` replaces manual text painting for simple labels. `ui.fonts(|f| f.layout_no_wrap(...))` for measuring text bounds before drawing. `egui::Vec2` is aliased at top level. `egui::pos2` and `egui::rect` for geometry. `ui.add_sized([w, h], widget)` for fixed-size widgets.
---

## [2026-07-30] - US-013
- What was implemented
  - **11 integration tests** in `lanremotecontrol/common/tests/common_integration.rs`:
    - `test_full_connection_handshake_simulated` — full 4-step handshake (request → capabilities → confirm → teardown) including rejected variant
    - `test_screen_encode_decode_pipeline` — full pipeline (frame creation → tile_checksums → detect_delta_tiles → build_delta_regions → compress_delta → decompress_delta → verify) with gradient frame and half-frame modification
    - `test_large_frame_4k_compression` — 3840×2160 checkerboard frame with timing measurements for tile checksums, LZ4 compress/decompress, delta compress/decompress, and compression ratio verification
    - `test_input_event_serialize_deserialize_pipeline` — all 4 input event types (KeyEvent, MouseMoveEvent, MouseButtonEvent, ScrollEvent) through full serialize→deserialize→verify pipeline with 8 test cases
    - `test_multi_message_stream_round_trip` — concatenated stream of 8 mixed messages (heartbeats, ACKs, connection management) verified round-trip
    - `test_large_payload_message` — messages with 5 different payload sizes (0, 1, MTU, 4×MTU, 100KB)
    - `test_captured_frame_to_encoding_pipeline` — CapturedFrame→encoding pipeline with simulated cursor change
    - `test_multiple_frames_sequential_delta` — 3-frame sequential delta detection (baseline → small cursor → larger change)
    - `test_various_encoding_capabilities_configs` — 4 encoding capability configurations serialized and verified
    - `test_message_boundary_conditions` — min message, max sequence number, all message types
    - `test_very_small_frame_encoding` — 100×100 frame edge case with single-pixel modification
  - **Benchmark document** `docs/BENCHMARK_RESULTS.md`:
    - Test environment documentation
    - Latency measurements for tile checksums, LZ4 compression/decompression (720p & 4K), delta operations
    - Compression ratios for various content types (gradient, checkerboard, uniform, delta)
    - Network protocol timing estimates
    - End-to-end pipeline latency estimates (3–10 ms best case on LAN)
    - 8 performance tuning recommendations: tile size adaptation, adaptive encoding selection, thread affinity, network buffer tuning, GPU direct transfer (DXGI), frame pacing, serialisation optimisation, build profile recommendations
- Files changed
  - `lanremotecontrol/common/tests/common_integration.rs` (NEW — 11 integration tests)
  - `docs/BENCHMARK_RESULTS.md` (NEW — benchmark methodology and results)
  - `prd.json` (US-013 passes: true)
- **Build result**: `cargo build` passes (same 6 warnings as before)
- **Test result**: All 68 tests pass (57 existing unit + 11 new integration) across all crates
- **Learnings for future iterations:**
  - Patterns discovered: Integration tests in `common/tests/` are auto-discovered by Cargo for library crates; bincode adds 22 bytes of framing overhead (4 enum tag + 4 seq + 4 payload_len + 2 reserved + 8 Vec length prefix), not the 11-byte protocol header_size; the serde `#[repr(u8)]` attribute on MessageType affects the wire format differently than bincode's own enum encoding
  - Gotchas encountered:
    1. bincode does NOT use `#[repr(u8)]` for enum serialization — it encodes enums as u32 (4 bytes) by default, not 1 byte, so the actual serialized message overhead is 22 bytes vs the 11-byte protocol header
    2. The concatenated stream test requires knowing each message's exact serialized length (from prior serialization) — cannot use `header_size() + payload_length` directly because bincode adds Vec length prefix
    3. 4K frame (3840×2160 → ~31.6 MB) round-trip through LZ4 works correctly in debug mode with reasonable performance (a few ms for compress+decompress)
    4. Checkerboard patterns compress very well with LZ4 (>10×), while gradient/random patterns may not compress much (<2×) — this is expected behavior for LZ4
  - Useful context: Cargo's `tests/` directory is auto-discovered for library crates; `cargo test -p lanremotecontrol-common --test common_integration` runs just the integration tests; `std::time::Instant` is adequate for millisecond-level benchmarks; delta detection with 64×64 tiles on 4K produces 2040 tiles, manageable in <20ms even in debug mode

## [2026-07-27 22:50:00] - US-002
- What was implemented
- Created docs/NETWORK_PROTOCOL.md with custom UDP-based network protocol design
- Defined message types (control commands, screen frames, ACKs, heartbeats, connection management)
- Specified sequence number and ACK mechanism for reliability
- Documented TOS/QoS settings for low delay (DSCP markings, socket options)
- **Learnings for future iterations:**
  - Patterns discovered: Network protocol uses UDP with application-level ACK for critical messages
  - Useful context: Control commands require ACK confirmation, screen frames are generally lossy to maintain low latency
## [2026-07-27 23:22:00] - US-005
- What was implemented
- Created docs/UI_UX_DESIGN.md with connection panel UI components (IP input, Connect button, status indicator)
- Documented remote control window UI (frame display area, fullscreen toggle, disconnect button)
- Defined interaction flow and state transitions for connection and remote control session
- **Learnings for future iterations:**
  - Patterns discovered: Connection panel uses minimal vertical stack layout with IP input, Connect button, and status indicator; Remote control window prioritizes maximum screen real estate with overlay controls that auto-hide after inactivity
  - Useful context: UI framework choices should prioritize low memory footprint and fast render times to avoid impacting remote control performance; aspect ratio preservation is maintained via black bars when client/host ratios differ
---
## [2026-07-30 16:15:00] - US-008
- What was implemented
  - Created Rust Cargo workspace under `lanremotecontrol/` with workspace root Cargo.toml
  - Implemented `common/` library crate with core message types (MessageType, Message, control command sub-types, connection management sub-types)
  - Implemented protocol constants (DEFAULT_PORT, HEARTBEAT_INTERVAL_MS, MAX_PACKET_SIZE, ACK_TIMEOUT_MS, MAX_RETRANSMIT)
  - Implemented session state types (ConnectionState enum, SessionConfig struct)
  - Implemented serialization using serde + bincode with round-trip tests
  - Created host binary crate (`host/`) and client binary crate (`client/`)
  - Added .gitignore for Rust build artifacts
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace root)
  - `lanremotecontrol/common/Cargo.toml`, `lanremotecontrol/common/src/lib.rs`
  - `lanremotecontrol/host/Cargo.toml`, `lanremotecontrol/host/src/main.rs`
  - `lanremotecontrol/client/Cargo.toml`, `lanremotecontrol/client/src/main.rs`
  - `lanremotecontrol/.gitignore`
- **Learnings for future iterations:**
  - Patterns discovered: Workspace resolver = "2" is needed for clean dependency resolution; use workspace-level [workspace.dependencies] to share dependency versions across packages
  - Gotchas encountered: bincode 1.x must be explicitly specified (3.x is latest but breaking); Message header size is 11 bytes (1+4+4+2) per protocol spec — important for MTU calculations
  - Useful context: The common crate is named `lanremotecontrol-common` to avoid naming conflicts; host and client crates depend on it via path dependency
---

## [2026-07-30] - US-010
- What was implemented
  - **Screen capture trait** in `common/src/capture.rs`: `ScreenCapture` trait, `CapturedFrame` struct, `CaptureError` enum
  - **Tile-based delta detection and LZ4 compression** in `common/src/encoding.rs`:
    - `tile_checksums()` — XXH32 hash per 64×64 tile
    - `detect_delta_tiles()` — find changed tiles between frames
    - `extract_tile_data()` — extract pixel data for a tile
    - `compress_delta()` / `decompress_delta()` — round-trip for delta regions
    - `compress_full_frame()` / `decompress_full_frame()` — full frame LZ4
    - `should_send_full_frame()` — threshold-based (>30%) fallback
    - `build_delta_regions()` — build delta regions from changed tiles
    - Comprehensive tests for encoding/decoding round-trips (all pass)
  - **Windows DXGI Desktop Duplication** in `host/src/capture.rs`:
    - `DxgiCapture` struct implementing `ScreenCapture` trait
    - D3D11 device creation (tries HW then WARP driver)
    - AcquireNextFrame with staging texture copy
    - Row-by-row pixel copy handling stride differences
    - `#[cfg(windows)]` guard with non-Windows stub returning `UnsupportedOs`
  - **Host main.rs demo**: initializes capture on startup, prints display info, tile counts, and LZ4 compression ratio
  - **Workspace dependencies**: Added `lz4`, `xxhash-rust`, `windows-core`, and `windows` (with `Win32_Graphics_Dxgi`, `Win32_Graphics_Direct3D`, `Win32_Graphics_Direct3D11`, `Win32_Graphics_Dxgi_Common`, `Win32_System_Com`, `Win32_Graphics_Gdi`, `Win32_Foundation`)
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace deps)
  - `lanremotecontrol/common/Cargo.toml`
  - `lanremotecontrol/host/Cargo.toml`
  - `lanremotecontrol/common/src/lib.rs` (added `pub mod capture;` and `pub mod encoding;`)
  - `lanremotecontrol/common/src/capture.rs` (new)
  - `lanremotecontrol/common/src/encoding.rs` (new)
  - `lanremotecontrol/host/src/capture.rs` (new)
  - `lanremotecontrol/host/src/main.rs` (added `mod capture;` and `demo_screen_capture()`)
  - `lanremotecontrol/Cargo.lock` (auto)
- **Learnings for future iterations:**
  - Patterns discovered: The `windows` crate v0.58 uses a distinctive COM API style where methods like `EnumOutputs(u32) -> Result<IDXGIOutput>` return the interface directly, not via output parameters. Types like `D3D_DRIVER_TYPE` live in `Win32_Graphics_Direct3D` module, NOT `Direct3D11`. The `Interface` trait with `.cast()` is in `windows::core::Interface`.
  - Gotchas encountered (Windows-specific DXGI issues!):
    1. `D3D_DRIVER_TYPE_HARDWARE` and `D3D_DRIVER_TYPE_WARP` are in `windows::Win32::Graphics::Direct3D` (not `Direct3D11`)
    2. `DuplicateOutput()` is on `IDXGIOutput1` (not `IDXGIOutput`) — need `.cast::<IDXGIOutput1>()` first
    3. `HMODULE` wrapper is required for the software rasterizer parameter: `HMODULE(std::ptr::null_mut())` instead of `None`
    4. `D3D11CreateDevice` signature: no separate `featurelevels: u32` param; `pfeaturelevels: None` handles both pointer and count
    5. `ID3D11Texture2D::GetDesc(&self, pdesc: *mut D3D11_TEXTURE2D_DESC)` takes an output pointer (returns `()`), not self-returning
    6. `D3D11_CPU_ACCESS_READ.0` is `i32` but `D3D11_TEXTURE2D_DESC.CPUAccessFlags` expects `u32` — need `.0 as u32` cast
    7. `BindFlags`, `CPUAccessFlags`, `MiscFlags` are raw `u32` fields, not the flag-wrapping types in the struct literal
  - Useful context: XXH32 is fast but for uniform frames edge tiles naturally have different hashes due to different data sizes; LZ4 may not compress high-entropy (pattern/random) data smaller than original — tests should verify round-trip correctness, not compression ratio
  - Updating dependencies: `windows-core` must be added as an explicit workspace + host dependency when using `windows::core::Interface`
---

## [2026-07-30] - US-011
- What was implemented
  - **Client-side input capture** in `client/src/input.rs`:
    - `InputCapture` struct with `GetAsyncKeyState` polling for all VK codes 0x01–0xFF
    - Keyboard state tracking: previous vs current to detect press/release transitions
    - Mouse position tracking via `GetCursorPos` with relative delta computation
    - Mouse button state tracking (left, right, middle, X1, X2) via `GetAsyncKeyState`
    - Modifier key state packing (Shift/Ctrl/Alt/Win) into u8 bitmask
    - High-resolution timestamp (microseconds since UNIX epoch) per event
    - Sequence number counter (`next_seq()`) for ordering
    - `#[cfg(windows)]` guard with non-Windows stub returning empty vectors
  - **Host-side input injection** in `host/src/input.rs`:
    - `InputInjector` struct with `inject()` and `inject_batch()` methods
    - Keyboard injection via `SendInput` with `KEYBDINPUT` (VIRTUAL_KEY, KEYBD_EVENT_FLAGS)
    - Mouse movement injection: relative (MOUSEEVENTF_MOVE) and absolute (MOUSEEVENTF_MOVE|MOUSEEVENTF_ABSOLUTE)
    - Mouse button injection: left/right/middle/X1/X2 down/up events
    - Scroll injection: vertical (MOUSEEVENTF_WHEEL) and horizontal (MOUSEEVENTF_HWHEEL)
    - 6 unit tests for payload-to-INPUT conversion logic
    - `#[cfg(windows)]` guard with non-Windows stub returning Unsupported error
- Files changed
  - `lanremotecontrol/Cargo.toml` (added `Win32_UI_Input_KeyboardAndMouse`, `Win32_UI_WindowsAndMessaging`)
  - `lanremotecontrol/client/Cargo.toml` (added `windows` dependency)
  - `lanremotecontrol/client/src/input.rs` (NEW — input capture module)
  - `lanremotecontrol/client/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/host/src/input.rs` (NEW — input injection module)
  - `lanremotecontrol/host/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-011 passes: true)
- **Learnings for future iterations:**
  - Patterns discovered: windows-rs v0.58 API — `GetAsyncKeyState(vkey: i32)` takes i32 not VIRTUAL_KEY; `GetCursorPos(lppoint: *mut POINT)` takes output pointer, returns Result<()>; `SendInput(pinputs: &[INPUT], cbsize: i32)` takes slice. `KEYBDINPUT.wVk` is VIRTUAL_KEY(u16), `dwFlags` is KEYBD_EVENT_FLAGS(u32). KEYEVENTF_KEYDOWN = 0 (no separate constant). `dwExtraInfo` is usize, not *mut c_void.
  - Gotchas encountered:
    1. `GetAsyncKeyState` takes `i32` directly (not `VIRTUAL_KEY`), even though `KEYBDINPUT.wVk` uses `VIRTUAL_KEY`
    2. `0x8000` literal doesn't fit in `i16` — must cast to `u16` first: `(state as u16) & 0x8000 != 0`
    3. `GetCursorPos` requires an output pointer `&mut POINT` and returns `Result<()>`, not directly the point
    4. `dwExtraInfo` in both `KEYBDINPUT` and `MOUSEINPUT` is `usize`, not `*mut c_void` — use `0usize`
    5. There is no `KEYEVENTF_KEYDOWN` constant in windows-rs 0.58 (value 0) — use `KEYBD_EVENT_FLAGS(0)`
  - Useful context: `INPUT_0` is a union-like struct with `mi`, `ki`, `hi` fields; using `Default::default()` zero-initializes it. `POINT` is in `windows::Win32::Foundation`. The SendInput batch approach (single call with many INPUTs) ensures atomic injection of related events (e.g., simultaneous mouse button + position).
---

## [2026-07-30] - US-012
- What was implemented
  - **Minimalist GUI** using egui 0.27 + eframe 0.27 in `client/src/ui.rs`:
    - `RemoteControlApp` struct implementing `eframe::App` trait
    - `AppState` enum: Disconnected, Connecting, Connected {encoding, width, height}, Error(String)
    - Connection panel with IP input field (TextEdit), port input field, colored status indicator, and Connect/Disconnect button
    - Remote control window with dark background, centered connection info text, FPS/latency info bottom-right
    - Fullscreen toggle and Disconnect overlay buttons in top-right corner
    - Error panel with retry button when handshake/connection fails
    - Async handshake via `std::thread::spawn` with `Arc<Mutex<Option<Result>>>` channel
    - `ctx.request_repaint()` for smooth animation during connecting state
  - **Updated `client/src/main.rs`**: replaced CLI logic with `eframe::run_native()` call
  - **Updated `client/Cargo.toml`**: added `eframe = { version = "0.27", features = ["default_system_theme", "glow"] }` and `egui = "0.27"`
  - Added `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` to hide console window in release builds
- Files changed
  - `lanremotecontrol/client/Cargo.toml` (added eframe, egui deps)
  - `lanremotecontrol/client/src/ui.rs` (NEW — egui UI module)
  - `lanremotecontrol/client/src/main.rs` (rewritten to use eframe)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-012 passes: true)
- **Build result**: `cargo build` passes with 6 warnings (unused imports/dead code from input.rs and net.rs — expected until full integration with US-013)
- **Test result**: All 57 tests pass (2 client + 39 common + 16 host)
- **Learnings for future iterations:**
  - Patterns discovered: eframe 0.27 uses `eframe::App::update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame)`. Use `egui::CentralPanel` for the main content area. `ui.vertical_centered()` with `egui::Frame::group()` for structured panels. `ctx.send_viewport_cmd(egui::ViewportCommand::Fullscreen(true/false))` for fullscreen toggling. `ctx.request_repaint()` needed for continuous updates during connecting state.
  - Gotchas encountered:
    1. eframe 0.27's `eframe::NativeOptions` has no `always_on_top`, `decorated` etc. directly — use `viewport: eframe::egui::ViewportBuilder::default()` for window properties
    2. `egui::ViewportCommand::Fullscreen(bool)` replaces the older `Frame::set_fullscreen()` API
    3. `egui::CentralPanel` is not an `egui::Panel` — it's a free function `egui::CentralPanel::default().show(ctx, |ui| {...})`
    4. For overlay buttons over the screen area, use `ui.put(rect, widget)` with absolute positions calculated from `ui.max_rect()`
    5. Can't borrow `self` mutably multiple times in `update()` — need to match on state clone or restructure logic to avoid borrow conflicts
  - Useful context: `egui::Color32::from_rgba_premultiplied(r,g,b,a)` for transparency on overlay buttons. `ui.colored_label()` replaces manual text painting for simple labels. `ui.fonts(|f| f.layout_no_wrap(...))` for measuring text bounds before drawing. `egui::Vec2` is aliased at top level. `egui::pos2` and `egui::rect` for geometry. `ui.add_sized([w, h], widget)` for fixed-size widgets.
---

## [2026-07-30] - US-013
- What was implemented
  - **11 integration tests** in `lanremotecontrol/common/tests/common_integration.rs`:
    - `test_full_connection_handshake_simulated` — full 4-step handshake (request → capabilities → confirm → teardown) including rejected variant
    - `test_screen_encode_decode_pipeline` — full pipeline (frame creation → tile_checksums → detect_delta_tiles → build_delta_regions → compress_delta → decompress_delta → verify) with gradient frame and half-frame modification
    - `test_large_frame_4k_compression` — 3840×2160 checkerboard frame with timing measurements for tile checksums, LZ4 compress/decompress, delta compress/decompress, and compression ratio verification
    - `test_input_event_serialize_deserialize_pipeline` — all 4 input event types (KeyEvent, MouseMoveEvent, MouseButtonEvent, ScrollEvent) through full serialize→deserialize→verify pipeline with 8 test cases
    - `test_multi_message_stream_round_trip` — concatenated stream of 8 mixed messages (heartbeats, ACKs, connection management) verified round-trip
    - `test_large_payload_message` — messages with 5 different payload sizes (0, 1, MTU, 4×MTU, 100KB)
    - `test_captured_frame_to_encoding_pipeline` — CapturedFrame→encoding pipeline with simulated cursor change
    - `test_multiple_frames_sequential_delta` — 3-frame sequential delta detection (baseline → small cursor → larger change)
    - `test_various_encoding_capabilities_configs` — 4 encoding capability configurations serialized and verified
    - `test_message_boundary_conditions` — min message, max sequence number, all message types
    - `test_very_small_frame_encoding` — 100×100 frame edge case with single-pixel modification
  - **Benchmark document** `docs/BENCHMARK_RESULTS.md`:
    - Test environment documentation
    - Latency measurements for tile checksums, LZ4 compression/decompression (720p & 4K), delta operations
    - Compression ratios for various content types (gradient, checkerboard, uniform, delta)
    - Network protocol timing estimates
    - End-to-end pipeline latency estimates (3–10 ms best case on LAN)
    - 8 performance tuning recommendations: tile size adaptation, adaptive encoding selection, thread affinity, network buffer tuning, GPU direct transfer (DXGI), frame pacing, serialisation optimisation, build profile recommendations
- Files changed
  - `lanremotecontrol/common/tests/common_integration.rs` (NEW — 11 integration tests)
  - `docs/BENCHMARK_RESULTS.md` (NEW — benchmark methodology and results)
  - `prd.json` (US-013 passes: true)
- **Build result**: `cargo build` passes (same 6 warnings as before)
- **Test result**: All 68 tests pass (57 existing unit + 11 new integration) across all crates
- **Learnings for future iterations:**
  - Patterns discovered: Integration tests in `common/tests/` are auto-discovered by Cargo for library crates; bincode adds 22 bytes of framing overhead (4 enum tag + 4 seq + 4 payload_len + 2 reserved + 8 Vec length prefix), not the 11-byte protocol header_size; the serde `#[repr(u8)]` attribute on MessageType affects the wire format differently than bincode's own enum encoding
  - Gotchas encountered:
    1. bincode does NOT use `#[repr(u8)]` for enum serialization — it encodes enums as u32 (4 bytes) by default, not 1 byte, so the actual serialized message overhead is 22 bytes vs the 11-byte protocol header
    2. The concatenated stream test requires knowing each message's exact serialized length (from prior serialization) — cannot use `header_size() + payload_length` directly because bincode adds Vec length prefix
    3. 4K frame (3840×2160 → ~31.6 MB) round-trip through LZ4 works correctly in debug mode with reasonable performance (a few ms for compress+decompress)
    4. Checkerboard patterns compress very well with LZ4 (>10×), while gradient/random patterns may not compress much (<2×) — this is expected behavior for LZ4
  - Useful context: Cargo's `tests/` directory is auto-discovered for library crates; `cargo test -p lanremotecontrol-common --test common_integration` runs just the integration tests; `std::time::Instant` is adequate for millisecond-level benchmarks; delta detection with 64×64 tiles on 4K produces 2040 tiles, manageable in <20ms even in debug mode

## [2026-07-27 23:12:00] - US-003
- What was implemented
- Created docs/SCREEN_CAPTURE_AND_ENCODING.md with OS-specific screen capture APIs (DXGI for Windows, ScreenCaptureKit for macOS, X11/Wayland for Linux)
- Defined delta frame detection algorithm using tile-based partitioning and hash-based change detection
- Defined compression strategy: Raw Delta + LZ4 Compression for minimal latency, and Low-Delay H.264/AV1 with hardware acceleration for high-motion or bandwidth-constrained scenarios
- **Learnings for future iterations:**
  - Patterns discovered: Screen capture uses OS-specific native APIs (DXGI on Windows, ScreenCaptureKit on macOS) to minimize overhead
  - Gotchas encountered: Delta frame detection must include a threshold-based full frame fallback (>30% tiles changed) to avoid excessive small patch transmission overhead
  - Useful context: Adaptive switching between LZ4 raw delta and low-delay H.264/AV1 is based on network RTT, packet loss, CPU/GPU utilization, and content type detection
## [2026-07-27 23:22:00] - US-005
- What was implemented
- Created docs/UI_UX_DESIGN.md with connection panel UI components (IP input, Connect button, status indicator)
- Documented remote control window UI (frame display area, fullscreen toggle, disconnect button)
- Defined interaction flow and state transitions for connection and remote control session
- **Learnings for future iterations:**
  - Patterns discovered: Connection panel uses minimal vertical stack layout with IP input, Connect button, and status indicator; Remote control window prioritizes maximum screen real estate with overlay controls that auto-hide after inactivity
  - Useful context: UI framework choices should prioritize low memory footprint and fast render times to avoid impacting remote control performance; aspect ratio preservation is maintained via black bars when client/host ratios differ
---
## [2026-07-30 16:15:00] - US-008
- What was implemented
  - Created Rust Cargo workspace under `lanremotecontrol/` with workspace root Cargo.toml
  - Implemented `common/` library crate with core message types (MessageType, Message, control command sub-types, connection management sub-types)
  - Implemented protocol constants (DEFAULT_PORT, HEARTBEAT_INTERVAL_MS, MAX_PACKET_SIZE, ACK_TIMEOUT_MS, MAX_RETRANSMIT)
  - Implemented session state types (ConnectionState enum, SessionConfig struct)
  - Implemented serialization using serde + bincode with round-trip tests
  - Created host binary crate (`host/`) and client binary crate (`client/`)
  - Added .gitignore for Rust build artifacts
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace root)
  - `lanremotecontrol/common/Cargo.toml`, `lanremotecontrol/common/src/lib.rs`
  - `lanremotecontrol/host/Cargo.toml`, `lanremotecontrol/host/src/main.rs`
  - `lanremotecontrol/client/Cargo.toml`, `lanremotecontrol/client/src/main.rs`
  - `lanremotecontrol/.gitignore`
- **Learnings for future iterations:**
  - Patterns discovered: Workspace resolver = "2" is needed for clean dependency resolution; use workspace-level [workspace.dependencies] to share dependency versions across packages
  - Gotchas encountered: bincode 1.x must be explicitly specified (3.x is latest but breaking); Message header size is 11 bytes (1+4+4+2) per protocol spec — important for MTU calculations
  - Useful context: The common crate is named `lanremotecontrol-common` to avoid naming conflicts; host and client crates depend on it via path dependency
---

## [2026-07-30] - US-010
- What was implemented
  - **Screen capture trait** in `common/src/capture.rs`: `ScreenCapture` trait, `CapturedFrame` struct, `CaptureError` enum
  - **Tile-based delta detection and LZ4 compression** in `common/src/encoding.rs`:
    - `tile_checksums()` — XXH32 hash per 64×64 tile
    - `detect_delta_tiles()` — find changed tiles between frames
    - `extract_tile_data()` — extract pixel data for a tile
    - `compress_delta()` / `decompress_delta()` — round-trip for delta regions
    - `compress_full_frame()` / `decompress_full_frame()` — full frame LZ4
    - `should_send_full_frame()` — threshold-based (>30%) fallback
    - `build_delta_regions()` — build delta regions from changed tiles
    - Comprehensive tests for encoding/decoding round-trips (all pass)
  - **Windows DXGI Desktop Duplication** in `host/src/capture.rs`:
    - `DxgiCapture` struct implementing `ScreenCapture` trait
    - D3D11 device creation (tries HW then WARP driver)
    - AcquireNextFrame with staging texture copy
    - Row-by-row pixel copy handling stride differences
    - `#[cfg(windows)]` guard with non-Windows stub returning `UnsupportedOs`
  - **Host main.rs demo**: initializes capture on startup, prints display info, tile counts, and LZ4 compression ratio
  - **Workspace dependencies**: Added `lz4`, `xxhash-rust`, `windows-core`, and `windows` (with `Win32_Graphics_Dxgi`, `Win32_Graphics_Direct3D`, `Win32_Graphics_Direct3D11`, `Win32_Graphics_Dxgi_Common`, `Win32_System_Com`, `Win32_Graphics_Gdi`, `Win32_Foundation`)
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace deps)
  - `lanremotecontrol/common/Cargo.toml`
  - `lanremotecontrol/host/Cargo.toml`
  - `lanremotecontrol/common/src/lib.rs` (added `pub mod capture;` and `pub mod encoding;`)
  - `lanremotecontrol/common/src/capture.rs` (new)
  - `lanremotecontrol/common/src/encoding.rs` (new)
  - `lanremotecontrol/host/src/capture.rs` (new)
  - `lanremotecontrol/host/src/main.rs` (added `mod capture;` and `demo_screen_capture()`)
  - `lanremotecontrol/Cargo.lock` (auto)
- **Learnings for future iterations:**
  - Patterns discovered: The `windows` crate v0.58 uses a distinctive COM API style where methods like `EnumOutputs(u32) -> Result<IDXGIOutput>` return the interface directly, not via output parameters. Types like `D3D_DRIVER_TYPE` live in `Win32_Graphics_Direct3D` module, NOT `Direct3D11`. The `Interface` trait with `.cast()` is in `windows::core::Interface`.
  - Gotchas encountered (Windows-specific DXGI issues!):
    1. `D3D_DRIVER_TYPE_HARDWARE` and `D3D_DRIVER_TYPE_WARP` are in `windows::Win32::Graphics::Direct3D` (not `Direct3D11`)
    2. `DuplicateOutput()` is on `IDXGIOutput1` (not `IDXGIOutput`) — need `.cast::<IDXGIOutput1>()` first
    3. `HMODULE` wrapper is required for the software rasterizer parameter: `HMODULE(std::ptr::null_mut())` instead of `None`
    4. `D3D11CreateDevice` signature: no separate `featurelevels: u32` param; `pfeaturelevels: None` handles both pointer and count
    5. `ID3D11Texture2D::GetDesc(&self, pdesc: *mut D3D11_TEXTURE2D_DESC)` takes an output pointer (returns `()`), not self-returning
    6. `D3D11_CPU_ACCESS_READ.0` is `i32` but `D3D11_TEXTURE2D_DESC.CPUAccessFlags` expects `u32` — need `.0 as u32` cast
    7. `BindFlags`, `CPUAccessFlags`, `MiscFlags` are raw `u32` fields, not the flag-wrapping types in the struct literal
  - Useful context: XXH32 is fast but for uniform frames edge tiles naturally have different hashes due to different data sizes; LZ4 may not compress high-entropy (pattern/random) data smaller than original — tests should verify round-trip correctness, not compression ratio
  - Updating dependencies: `windows-core` must be added as an explicit workspace + host dependency when using `windows::core::Interface`
---

## [2026-07-30] - US-011
- What was implemented
  - **Client-side input capture** in `client/src/input.rs`:
    - `InputCapture` struct with `GetAsyncKeyState` polling for all VK codes 0x01–0xFF
    - Keyboard state tracking: previous vs current to detect press/release transitions
    - Mouse position tracking via `GetCursorPos` with relative delta computation
    - Mouse button state tracking (left, right, middle, X1, X2) via `GetAsyncKeyState`
    - Modifier key state packing (Shift/Ctrl/Alt/Win) into u8 bitmask
    - High-resolution timestamp (microseconds since UNIX epoch) per event
    - Sequence number counter (`next_seq()`) for ordering
    - `#[cfg(windows)]` guard with non-Windows stub returning empty vectors
  - **Host-side input injection** in `host/src/input.rs`:
    - `InputInjector` struct with `inject()` and `inject_batch()` methods
    - Keyboard injection via `SendInput` with `KEYBDINPUT` (VIRTUAL_KEY, KEYBD_EVENT_FLAGS)
    - Mouse movement injection: relative (MOUSEEVENTF_MOVE) and absolute (MOUSEEVENTF_MOVE|MOUSEEVENTF_ABSOLUTE)
    - Mouse button injection: left/right/middle/X1/X2 down/up events
    - Scroll injection: vertical (MOUSEEVENTF_WHEEL) and horizontal (MOUSEEVENTF_HWHEEL)
    - 6 unit tests for payload-to-INPUT conversion logic
    - `#[cfg(windows)]` guard with non-Windows stub returning Unsupported error
- Files changed
  - `lanremotecontrol/Cargo.toml` (added `Win32_UI_Input_KeyboardAndMouse`, `Win32_UI_WindowsAndMessaging`)
  - `lanremotecontrol/client/Cargo.toml` (added `windows` dependency)
  - `lanremotecontrol/client/src/input.rs` (NEW — input capture module)
  - `lanremotecontrol/client/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/host/src/input.rs` (NEW — input injection module)
  - `lanremotecontrol/host/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-011 passes: true)
- **Learnings for future iterations:**
  - Patterns discovered: windows-rs v0.58 API — `GetAsyncKeyState(vkey: i32)` takes i32 not VIRTUAL_KEY; `GetCursorPos(lppoint: *mut POINT)` takes output pointer, returns Result<()>; `SendInput(pinputs: &[INPUT], cbsize: i32)` takes slice. `KEYBDINPUT.wVk` is VIRTUAL_KEY(u16), `dwFlags` is KEYBD_EVENT_FLAGS(u32). KEYEVENTF_KEYDOWN = 0 (no separate constant). `dwExtraInfo` is usize, not *mut c_void.
  - Gotchas encountered:
    1. `GetAsyncKeyState` takes `i32` directly (not `VIRTUAL_KEY`), even though `KEYBDINPUT.wVk` uses `VIRTUAL_KEY`
    2. `0x8000` literal doesn't fit in `i16` — must cast to `u16` first: `(state as u16) & 0x8000 != 0`
    3. `GetCursorPos` requires an output pointer `&mut POINT` and returns `Result<()>`, not directly the point
    4. `dwExtraInfo` in both `KEYBDINPUT` and `MOUSEINPUT` is `usize`, not `*mut c_void` — use `0usize`
    5. There is no `KEYEVENTF_KEYDOWN` constant in windows-rs 0.58 (value 0) — use `KEYBD_EVENT_FLAGS(0)`
  - Useful context: `INPUT_0` is a union-like struct with `mi`, `ki`, `hi` fields; using `Default::default()` zero-initializes it. `POINT` is in `windows::Win32::Foundation`. The SendInput batch approach (single call with many INPUTs) ensures atomic injection of related events (e.g., simultaneous mouse button + position).
---

## [2026-07-30] - US-012
- What was implemented
  - **Minimalist GUI** using egui 0.27 + eframe 0.27 in `client/src/ui.rs`:
    - `RemoteControlApp` struct implementing `eframe::App` trait
    - `AppState` enum: Disconnected, Connecting, Connected {encoding, width, height}, Error(String)
    - Connection panel with IP input field (TextEdit), port input field, colored status indicator, and Connect/Disconnect button
    - Remote control window with dark background, centered connection info text, FPS/latency info bottom-right
    - Fullscreen toggle and Disconnect overlay buttons in top-right corner
    - Error panel with retry button when handshake/connection fails
    - Async handshake via `std::thread::spawn` with `Arc<Mutex<Option<Result>>>` channel
    - `ctx.request_repaint()` for smooth animation during connecting state
  - **Updated `client/src/main.rs`**: replaced CLI logic with `eframe::run_native()` call
  - **Updated `client/Cargo.toml`**: added `eframe = { version = "0.27", features = ["default_system_theme", "glow"] }` and `egui = "0.27"`
  - Added `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` to hide console window in release builds
- Files changed
  - `lanremotecontrol/client/Cargo.toml` (added eframe, egui deps)
  - `lanremotecontrol/client/src/ui.rs` (NEW — egui UI module)
  - `lanremotecontrol/client/src/main.rs` (rewritten to use eframe)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-012 passes: true)
- **Build result**: `cargo build` passes with 6 warnings (unused imports/dead code from input.rs and net.rs — expected until full integration with US-013)
- **Test result**: All 57 tests pass (2 client + 39 common + 16 host)
- **Learnings for future iterations:**
  - Patterns discovered: eframe 0.27 uses `eframe::App::update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame)`. Use `egui::CentralPanel` for the main content area. `ui.vertical_centered()` with `egui::Frame::group()` for structured panels. `ctx.send_viewport_cmd(egui::ViewportCommand::Fullscreen(true/false))` for fullscreen toggling. `ctx.request_repaint()` needed for continuous updates during connecting state.
  - Gotchas encountered:
    1. eframe 0.27's `eframe::NativeOptions` has no `always_on_top`, `decorated` etc. directly — use `viewport: eframe::egui::ViewportBuilder::default()` for window properties
    2. `egui::ViewportCommand::Fullscreen(bool)` replaces the older `Frame::set_fullscreen()` API
    3. `egui::CentralPanel` is not an `egui::Panel` — it's a free function `egui::CentralPanel::default().show(ctx, |ui| {...})`
    4. For overlay buttons over the screen area, use `ui.put(rect, widget)` with absolute positions calculated from `ui.max_rect()`
    5. Can't borrow `self` mutably multiple times in `update()` — need to match on state clone or restructure logic to avoid borrow conflicts
  - Useful context: `egui::Color32::from_rgba_premultiplied(r,g,b,a)` for transparency on overlay buttons. `ui.colored_label()` replaces manual text painting for simple labels. `ui.fonts(|f| f.layout_no_wrap(...))` for measuring text bounds before drawing. `egui::Vec2` is aliased at top level. `egui::pos2` and `egui::rect` for geometry. `ui.add_sized([w, h], widget)` for fixed-size widgets.
---

## [2026-07-30] - US-013
- What was implemented
  - **11 integration tests** in `lanremotecontrol/common/tests/common_integration.rs`:
    - `test_full_connection_handshake_simulated` — full 4-step handshake (request → capabilities → confirm → teardown) including rejected variant
    - `test_screen_encode_decode_pipeline` — full pipeline (frame creation → tile_checksums → detect_delta_tiles → build_delta_regions → compress_delta → decompress_delta → verify) with gradient frame and half-frame modification
    - `test_large_frame_4k_compression` — 3840×2160 checkerboard frame with timing measurements for tile checksums, LZ4 compress/decompress, delta compress/decompress, and compression ratio verification
    - `test_input_event_serialize_deserialize_pipeline` — all 4 input event types (KeyEvent, MouseMoveEvent, MouseButtonEvent, ScrollEvent) through full serialize→deserialize→verify pipeline with 8 test cases
    - `test_multi_message_stream_round_trip` — concatenated stream of 8 mixed messages (heartbeats, ACKs, connection management) verified round-trip
    - `test_large_payload_message` — messages with 5 different payload sizes (0, 1, MTU, 4×MTU, 100KB)
    - `test_captured_frame_to_encoding_pipeline` — CapturedFrame→encoding pipeline with simulated cursor change
    - `test_multiple_frames_sequential_delta` — 3-frame sequential delta detection (baseline → small cursor → larger change)
    - `test_various_encoding_capabilities_configs` — 4 encoding capability configurations serialized and verified
    - `test_message_boundary_conditions` — min message, max sequence number, all message types
    - `test_very_small_frame_encoding` — 100×100 frame edge case with single-pixel modification
  - **Benchmark document** `docs/BENCHMARK_RESULTS.md`:
    - Test environment documentation
    - Latency measurements for tile checksums, LZ4 compression/decompression (720p & 4K), delta operations
    - Compression ratios for various content types (gradient, checkerboard, uniform, delta)
    - Network protocol timing estimates
    - End-to-end pipeline latency estimates (3–10 ms best case on LAN)
    - 8 performance tuning recommendations: tile size adaptation, adaptive encoding selection, thread affinity, network buffer tuning, GPU direct transfer (DXGI), frame pacing, serialisation optimisation, build profile recommendations
- Files changed
  - `lanremotecontrol/common/tests/common_integration.rs` (NEW — 11 integration tests)
  - `docs/BENCHMARK_RESULTS.md` (NEW — benchmark methodology and results)
  - `prd.json` (US-013 passes: true)
- **Build result**: `cargo build` passes (same 6 warnings as before)
- **Test result**: All 68 tests pass (57 existing unit + 11 new integration) across all crates
- **Learnings for future iterations:**
  - Patterns discovered: Integration tests in `common/tests/` are auto-discovered by Cargo for library crates; bincode adds 22 bytes of framing overhead (4 enum tag + 4 seq + 4 payload_len + 2 reserved + 8 Vec length prefix), not the 11-byte protocol header_size; the serde `#[repr(u8)]` attribute on MessageType affects the wire format differently than bincode's own enum encoding
  - Gotchas encountered:
    1. bincode does NOT use `#[repr(u8)]` for enum serialization — it encodes enums as u32 (4 bytes) by default, not 1 byte, so the actual serialized message overhead is 22 bytes vs the 11-byte protocol header
    2. The concatenated stream test requires knowing each message's exact serialized length (from prior serialization) — cannot use `header_size() + payload_length` directly because bincode adds Vec length prefix
    3. 4K frame (3840×2160 → ~31.6 MB) round-trip through LZ4 works correctly in debug mode with reasonable performance (a few ms for compress+decompress)
    4. Checkerboard patterns compress very well with LZ4 (>10×), while gradient/random patterns may not compress much (<2×) — this is expected behavior for LZ4
  - Useful context: Cargo's `tests/` directory is auto-discovered for library crates; `cargo test -p lanremotecontrol-common --test common_integration` runs just the integration tests; `std::time::Instant` is adequate for millisecond-level benchmarks; delta detection with 64×64 tiles on 4K produces 2040 tiles, manageable in <20ms even in debug mode

## [2026-07-27 23:15:00] - US-004
- What was implemented
- Created docs/INPUT_FORWARDING.md with client-side input capture mechanisms and host-side low-level input injection design
- Documented local input event capture on client (Windows WH_KEYBOARD_LL/WH_MOUSE_LL, macOS CGEventTap, Linux evdev/X11)
- Specified OS-specific injection API (Windows SendInput, macOS CGPostKeyboardEvent/CGPostMouseEvent, Linux XTestFakeKeyEvent/evdev)
- Defined timestamping and ordering rules for input events (high-resolution timestamps, monotonically increasing sequence numbers, UDP packet reordering and coalescing rules)
- **Learnings for future iterations:**
  - Patterns discovered: Input capture uses OS-specific low-level APIs (WH_KEYBOARD_LL/WH_MOUSE_LL on Windows, CGEventTap on macOS, evdev/X11 on Linux)
  - Gotchas encountered: Host input injection via SendInput requires proper foreground focus handling; modifier key state synchronization is critical to prevent stuck keys due to UDP packet loss
  - Useful context: Input events are serialized with type, high-resolution timestamp, and sequence numbers; host applies coalescing for rapid mouse movements and maintains key state maps to handle UDP unordered/duplicated packets
## [2026-07-27 23:22:00] - US-005
- What was implemented
- Created docs/UI_UX_DESIGN.md with connection panel UI components (IP input, Connect button, status indicator)
- Documented remote control window UI (frame display area, fullscreen toggle, disconnect button)
- Defined interaction flow and state transitions for connection and remote control session
- **Learnings for future iterations:**
  - Patterns discovered: Connection panel uses minimal vertical stack layout with IP input, Connect button, and status indicator; Remote control window prioritizes maximum screen real estate with overlay controls that auto-hide after inactivity
  - Useful context: UI framework choices should prioritize low memory footprint and fast render times to avoid impacting remote control performance; aspect ratio preservation is maintained via black bars when client/host ratios differ
---
## [2026-07-30 16:15:00] - US-008
- What was implemented
  - Created Rust Cargo workspace under `lanremotecontrol/` with workspace root Cargo.toml
  - Implemented `common/` library crate with core message types (MessageType, Message, control command sub-types, connection management sub-types)
  - Implemented protocol constants (DEFAULT_PORT, HEARTBEAT_INTERVAL_MS, MAX_PACKET_SIZE, ACK_TIMEOUT_MS, MAX_RETRANSMIT)
  - Implemented session state types (ConnectionState enum, SessionConfig struct)
  - Implemented serialization using serde + bincode with round-trip tests
  - Created host binary crate (`host/`) and client binary crate (`client/`)
  - Added .gitignore for Rust build artifacts
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace root)
  - `lanremotecontrol/common/Cargo.toml`, `lanremotecontrol/common/src/lib.rs`
  - `lanremotecontrol/host/Cargo.toml`, `lanremotecontrol/host/src/main.rs`
  - `lanremotecontrol/client/Cargo.toml`, `lanremotecontrol/client/src/main.rs`
  - `lanremotecontrol/.gitignore`
- **Learnings for future iterations:**
  - Patterns discovered: Workspace resolver = "2" is needed for clean dependency resolution; use workspace-level [workspace.dependencies] to share dependency versions across packages
  - Gotchas encountered: bincode 1.x must be explicitly specified (3.x is latest but breaking); Message header size is 11 bytes (1+4+4+2) per protocol spec — important for MTU calculations
  - Useful context: The common crate is named `lanremotecontrol-common` to avoid naming conflicts; host and client crates depend on it via path dependency
---

## [2026-07-30] - US-010
- What was implemented
  - **Screen capture trait** in `common/src/capture.rs`: `ScreenCapture` trait, `CapturedFrame` struct, `CaptureError` enum
  - **Tile-based delta detection and LZ4 compression** in `common/src/encoding.rs`:
    - `tile_checksums()` — XXH32 hash per 64×64 tile
    - `detect_delta_tiles()` — find changed tiles between frames
    - `extract_tile_data()` — extract pixel data for a tile
    - `compress_delta()` / `decompress_delta()` — round-trip for delta regions
    - `compress_full_frame()` / `decompress_full_frame()` — full frame LZ4
    - `should_send_full_frame()` — threshold-based (>30%) fallback
    - `build_delta_regions()` — build delta regions from changed tiles
    - Comprehensive tests for encoding/decoding round-trips (all pass)
  - **Windows DXGI Desktop Duplication** in `host/src/capture.rs`:
    - `DxgiCapture` struct implementing `ScreenCapture` trait
    - D3D11 device creation (tries HW then WARP driver)
    - AcquireNextFrame with staging texture copy
    - Row-by-row pixel copy handling stride differences
    - `#[cfg(windows)]` guard with non-Windows stub returning `UnsupportedOs`
  - **Host main.rs demo**: initializes capture on startup, prints display info, tile counts, and LZ4 compression ratio
  - **Workspace dependencies**: Added `lz4`, `xxhash-rust`, `windows-core`, and `windows` (with `Win32_Graphics_Dxgi`, `Win32_Graphics_Direct3D`, `Win32_Graphics_Direct3D11`, `Win32_Graphics_Dxgi_Common`, `Win32_System_Com`, `Win32_Graphics_Gdi`, `Win32_Foundation`)
- Files changed
  - `lanremotecontrol/Cargo.toml` (workspace deps)
  - `lanremotecontrol/common/Cargo.toml`
  - `lanremotecontrol/host/Cargo.toml`
  - `lanremotecontrol/common/src/lib.rs` (added `pub mod capture;` and `pub mod encoding;`)
  - `lanremotecontrol/common/src/capture.rs` (new)
  - `lanremotecontrol/common/src/encoding.rs` (new)
  - `lanremotecontrol/host/src/capture.rs` (new)
  - `lanremotecontrol/host/src/main.rs` (added `mod capture;` and `demo_screen_capture()`)
  - `lanremotecontrol/Cargo.lock` (auto)
- **Learnings for future iterations:**
  - Patterns discovered: The `windows` crate v0.58 uses a distinctive COM API style where methods like `EnumOutputs(u32) -> Result<IDXGIOutput>` return the interface directly, not via output parameters. Types like `D3D_DRIVER_TYPE` live in `Win32_Graphics_Direct3D` module, NOT `Direct3D11`. The `Interface` trait with `.cast()` is in `windows::core::Interface`.
  - Gotchas encountered (Windows-specific DXGI issues!):
    1. `D3D_DRIVER_TYPE_HARDWARE` and `D3D_DRIVER_TYPE_WARP` are in `windows::Win32::Graphics::Direct3D` (not `Direct3D11`)
    2. `DuplicateOutput()` is on `IDXGIOutput1` (not `IDXGIOutput`) — need `.cast::<IDXGIOutput1>()` first
    3. `HMODULE` wrapper is required for the software rasterizer parameter: `HMODULE(std::ptr::null_mut())` instead of `None`
    4. `D3D11CreateDevice` signature: no separate `featurelevels: u32` param; `pfeaturelevels: None` handles both pointer and count
    5. `ID3D11Texture2D::GetDesc(&self, pdesc: *mut D3D11_TEXTURE2D_DESC)` takes an output pointer (returns `()`), not self-returning
    6. `D3D11_CPU_ACCESS_READ.0` is `i32` but `D3D11_TEXTURE2D_DESC.CPUAccessFlags` expects `u32` — need `.0 as u32` cast
    7. `BindFlags`, `CPUAccessFlags`, `MiscFlags` are raw `u32` fields, not the flag-wrapping types in the struct literal
  - Useful context: XXH32 is fast but for uniform frames edge tiles naturally have different hashes due to different data sizes; LZ4 may not compress high-entropy (pattern/random) data smaller than original — tests should verify round-trip correctness, not compression ratio
  - Updating dependencies: `windows-core` must be added as an explicit workspace + host dependency when using `windows::core::Interface`
---

## [2026-07-30] - US-011
- What was implemented
  - **Client-side input capture** in `client/src/input.rs`:
    - `InputCapture` struct with `GetAsyncKeyState` polling for all VK codes 0x01–0xFF
    - Keyboard state tracking: previous vs current to detect press/release transitions
    - Mouse position tracking via `GetCursorPos` with relative delta computation
    - Mouse button state tracking (left, right, middle, X1, X2) via `GetAsyncKeyState`
    - Modifier key state packing (Shift/Ctrl/Alt/Win) into u8 bitmask
    - High-resolution timestamp (microseconds since UNIX epoch) per event
    - Sequence number counter (`next_seq()`) for ordering
    - `#[cfg(windows)]` guard with non-Windows stub returning empty vectors
  - **Host-side input injection** in `host/src/input.rs`:
    - `InputInjector` struct with `inject()` and `inject_batch()` methods
    - Keyboard injection via `SendInput` with `KEYBDINPUT` (VIRTUAL_KEY, KEYBD_EVENT_FLAGS)
    - Mouse movement injection: relative (MOUSEEVENTF_MOVE) and absolute (MOUSEEVENTF_MOVE|MOUSEEVENTF_ABSOLUTE)
    - Mouse button injection: left/right/middle/X1/X2 down/up events
    - Scroll injection: vertical (MOUSEEVENTF_WHEEL) and horizontal (MOUSEEVENTF_HWHEEL)
    - 6 unit tests for payload-to-INPUT conversion logic
    - `#[cfg(windows)]` guard with non-Windows stub returning Unsupported error
- Files changed
  - `lanremotecontrol/Cargo.toml` (added `Win32_UI_Input_KeyboardAndMouse`, `Win32_UI_WindowsAndMessaging`)
  - `lanremotecontrol/client/Cargo.toml` (added `windows` dependency)
  - `lanremotecontrol/client/src/input.rs` (NEW — input capture module)
  - `lanremotecontrol/client/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/host/src/input.rs` (NEW — input injection module)
  - `lanremotecontrol/host/src/main.rs` (added `mod input;`)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-011 passes: true)
- **Learnings for future iterations:**
  - Patterns discovered: windows-rs v0.58 API — `GetAsyncKeyState(vkey: i32)` takes i32 not VIRTUAL_KEY; `GetCursorPos(lppoint: *mut POINT)` takes output pointer, returns Result<()>; `SendInput(pinputs: &[INPUT], cbsize: i32)` takes slice. `KEYBDINPUT.wVk` is VIRTUAL_KEY(u16), `dwFlags` is KEYBD_EVENT_FLAGS(u32). KEYEVENTF_KEYDOWN = 0 (no separate constant). `dwExtraInfo` is usize, not *mut c_void.
  - Gotchas encountered:
    1. `GetAsyncKeyState` takes `i32` directly (not `VIRTUAL_KEY`), even though `KEYBDINPUT.wVk` uses `VIRTUAL_KEY`
    2. `0x8000` literal doesn't fit in `i16` — must cast to `u16` first: `(state as u16) & 0x8000 != 0`
    3. `GetCursorPos` requires an output pointer `&mut POINT` and returns `Result<()>`, not directly the point
    4. `dwExtraInfo` in both `KEYBDINPUT` and `MOUSEINPUT` is `usize`, not `*mut c_void` — use `0usize`
    5. There is no `KEYEVENTF_KEYDOWN` constant in windows-rs 0.58 (value 0) — use `KEYBD_EVENT_FLAGS(0)`
  - Useful context: `INPUT_0` is a union-like struct with `mi`, `ki`, `hi` fields; using `Default::default()` zero-initializes it. `POINT` is in `windows::Win32::Foundation`. The SendInput batch approach (single call with many INPUTs) ensures atomic injection of related events (e.g., simultaneous mouse button + position).
---

## [2026-07-30] - US-012
- What was implemented
  - **Minimalist GUI** using egui 0.27 + eframe 0.27 in `client/src/ui.rs`:
    - `RemoteControlApp` struct implementing `eframe::App` trait
    - `AppState` enum: Disconnected, Connecting, Connected {encoding, width, height}, Error(String)
    - Connection panel with IP input field (TextEdit), port input field, colored status indicator, and Connect/Disconnect button
    - Remote control window with dark background, centered connection info text, FPS/latency info bottom-right
    - Fullscreen toggle and Disconnect overlay buttons in top-right corner
    - Error panel with retry button when handshake/connection fails
    - Async handshake via `std::thread::spawn` with `Arc<Mutex<Option<Result>>>` channel
    - `ctx.request_repaint()` for smooth animation during connecting state
  - **Updated `client/src/main.rs`**: replaced CLI logic with `eframe::run_native()` call
  - **Updated `client/Cargo.toml`**: added `eframe = { version = "0.27", features = ["default_system_theme", "glow"] }` and `egui = "0.27"`
  - Added `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` to hide console window in release builds
- Files changed
  - `lanremotecontrol/client/Cargo.toml` (added eframe, egui deps)
  - `lanremotecontrol/client/src/ui.rs` (NEW — egui UI module)
  - `lanremotecontrol/client/src/main.rs` (rewritten to use eframe)
  - `lanremotecontrol/Cargo.lock` (auto)
  - `prd.json` (US-012 passes: true)
- **Build result**: `cargo build` passes with 6 warnings (unused imports/dead code from input.rs and net.rs — expected until full integration with US-013)
- **Test result**: All 57 tests pass (2 client + 39 common + 16 host)
- **Learnings for future iterations:**
  - Patterns discovered: eframe 0.27 uses `eframe::App::update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame)`. Use `egui::CentralPanel` for the main content area. `ui.vertical_centered()` with `egui::Frame::group()` for structured panels. `ctx.send_viewport_cmd(egui::ViewportCommand::Fullscreen(true/false))` for fullscreen toggling. `ctx.request_repaint()` needed for continuous updates during connecting state.
  - Gotchas encountered:
    1. eframe 0.27's `eframe::NativeOptions` has no `always_on_top`, `decorated` etc. directly — use `viewport: eframe::egui::ViewportBuilder::default()` for window properties
    2. `egui::ViewportCommand::Fullscreen(bool)` replaces the older `Frame::set_fullscreen()` API
    3. `egui::CentralPanel` is not an `egui::Panel` — it's a free function `egui::CentralPanel::default().show(ctx, |ui| {...})`
    4. For overlay buttons over the screen area, use `ui.put(rect, widget)` with absolute positions calculated from `ui.max_rect()`
    5. Can't borrow `self` mutably multiple times in `update()` — need to match on state clone or restructure logic to avoid borrow conflicts
  - Useful context: `egui::Color32::from_rgba_premultiplied(r,g,b,a)` for transparency on overlay buttons. `ui.colored_label()` replaces manual text painting for simple labels. `ui.fonts(|f| f.layout_no_wrap(...))` for measuring text bounds before drawing. `egui::Vec2` is aliased at top level. `egui::pos2` and `egui::rect` for geometry. `ui.add_sized([w, h], widget)` for fixed-size widgets.
---

## [2026-07-30] - US-013
- What was implemented
  - **11 integration tests** in `lanremotecontrol/common/tests/common_integration.rs`:
    - `test_full_connection_handshake_simulated` — full 4-step handshake (request → capabilities → confirm → teardown) including rejected variant
    - `test_screen_encode_decode_pipeline` — full pipeline (frame creation → tile_checksums → detect_delta_tiles → build_delta_regions → compress_delta → decompress_delta → verify) with gradient frame and half-frame modification
    - `test_large_frame_4k_compression` — 3840×2160 checkerboard frame with timing measurements for tile checksums, LZ4 compress/decompress, delta compress/decompress, and compression ratio verification
    - `test_input_event_serialize_deserialize_pipeline` — all 4 input event types (KeyEvent, MouseMoveEvent, MouseButtonEvent, ScrollEvent) through full serialize→deserialize→verify pipeline with 8 test cases
    - `test_multi_message_stream_round_trip` — concatenated stream of 8 mixed messages (heartbeats, ACKs, connection management) verified round-trip
    - `test_large_payload_message` — messages with 5 different payload sizes (0, 1, MTU, 4×MTU, 100KB)
    - `test_captured_frame_to_encoding_pipeline` — CapturedFrame→encoding pipeline with simulated cursor change
    - `test_multiple_frames_sequential_delta` — 3-frame sequential delta detection (baseline → small cursor → larger change)
    - `test_various_encoding_capabilities_configs` — 4 encoding capability configurations serialized and verified
    - `test_message_boundary_conditions` — min message, max sequence number, all message types
    - `test_very_small_frame_encoding` — 100×100 frame edge case with single-pixel modification
  - **Benchmark document** `docs/BENCHMARK_RESULTS.md`:
    - Test environment documentation
    - Latency measurements for tile checksums, LZ4 compression/decompression (720p & 4K), delta operations
    - Compression ratios for various content types (gradient, checkerboard, uniform, delta)
    - Network protocol timing estimates
    - End-to-end pipeline latency estimates (3–10 ms best case on LAN)
    - 8 performance tuning recommendations: tile size adaptation, adaptive encoding selection, thread affinity, network buffer tuning, GPU direct transfer (DXGI), frame pacing, serialisation optimisation, build profile recommendations
- Files changed
  - `lanremotecontrol/common/tests/common_integration.rs` (NEW — 11 integration tests)
  - `docs/BENCHMARK_RESULTS.md` (NEW — benchmark methodology and results)
  - `prd.json` (US-013 passes: true)
- **Build result**: `cargo build` passes (same 6 warnings as before)
- **Test result**: All 68 tests pass (57 existing unit + 11 new integration) across all crates
- **Learnings for future iterations:**
  - Patterns discovered: Integration tests in `common/tests/` are auto-discovered by Cargo for library crates; bincode adds 22 bytes of framing overhead (4 enum tag + 4 seq + 4 payload_len + 2 reserved + 8 Vec length prefix), not the 11-byte protocol header_size; the serde `#[repr(u8)]` attribute on MessageType affects the wire format differently than bincode's own enum encoding
  - Gotchas encountered:
    1. bincode does NOT use `#[repr(u8)]` for enum serialization — it encodes enums as u32 (4 bytes) by default, not 1 byte, so the actual serialized message overhead is 22 bytes vs the 11-byte protocol header
    2. The concatenated stream test requires knowing each message's exact serialized length (from prior serialization) — cannot use `header_size() + payload_length` directly because bincode adds Vec length prefix
    3. 4K frame (3840×2160 → ~31.6 MB) round-trip through LZ4 works correctly in debug mode with reasonable performance (a few ms for compress+decompress)
    4. Checkerboard patterns compress very well with LZ4 (>10×), while gradient/random patterns may not compress much (<2×) — this is expected behavior for LZ4
  - Useful context: Cargo's `tests/` directory is auto-discovered for library crates; `cargo test -p lanremotecontrol-common --test common_integration` runs just the integration tests; `std::time::Instant` is adequate for millisecond-level benchmarks; delta detection with 64×64 tiles on 4K produces 2040 tiles, manageable in <20ms even in debug mode

